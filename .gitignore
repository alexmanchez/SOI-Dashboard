# Dependencies (regenerated by `npm install`)
node_modules

# Build output
dist

# Environment / secrets (copy .env.example to .env)
.env
.env.local
.env.*.local

# Editor / OS / log noise
.DS_Store
*.log
.vscode
.idea

# Catena helper scripts (local-only)
push-to-github.bat

# One-shot refactor scripts
_extract.py
_rewrite.py
_split_components.py
_split_pages.py
_sweep_unused.py
_strip_react.py
_debug.py

# Claude Code artifacts — keep only the project-level launch config in
# version control; everything else (worktrees, sessions, cache, settings,
# transcripts) is per-developer scratch state.
.claude/
!.claude/launch.json

# Personal calendar file accidentally committed in an early sprint
Reminder.ics
