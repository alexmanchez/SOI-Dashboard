# Dependencies (regenerated by `npm install`)
node_modules

# Build output
dist

# Environment / secrets (copy .env.example to .env)
.env
.env.local
.env.*.local

# Editor / OS / log noise
.DS_Store
*.log
.vscode
.idea

# Catena helper scripts (local-only)
push-to-github.bat

# One-shot refactor scripts
_extract.py
_rewrite.py
_split_components.py
_split_pages.py

# Local-only Claude Code settings
.claude/settings.local.json
